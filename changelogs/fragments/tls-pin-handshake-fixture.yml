---
trivial:
  - >-
    tests/unit - add an in-process, self-signed TLS 1.2 server fixture using
    this board's cipher set, so the fingerprint-pin, C(validate_certs=false),
    and ordinary-CA-validation trust modes are each exercised against a real
    handshake rather than only asserted at the C(requests) mock boundary. This
    is test coverage only; the fix it proves out
    (C(AmiLegacyTlsAdapter.cert_verify) preserving the resolved trust mode) is
    already released, see C(changelogs/fragments/live-tls-csrf.yml).
